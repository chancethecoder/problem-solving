#include <iostream>
#include <cstdio>
#include <vector>

using namespace std;
const int MAX_N = 32000;
int n, m;
vector<int> adj[MAX_N];

void proc()
{
    cin >> n >> m;

    for(int i = 0; i < m; ++i)
    {
    }
}

int main()
{
    ios_base::sync_with_stdio(false);
    proc();
    return 0;
}
